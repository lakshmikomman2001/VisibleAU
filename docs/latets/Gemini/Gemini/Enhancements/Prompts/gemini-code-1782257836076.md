# VisibleAU Phase 2 LLD Addendum — Advanced Market-Moat Enhancements

**Target Audience:** Programmatic Build Context for Claude Code  
**System Layer:** Layer 1 (Retrieval) & Layer 2 (Visibility) Infrastructure Extensions  
**Design Posture:** 100% Additive. Strictly preserves Phase 1 Sprints 1–10 schema fields, test suites, and invariants[cite: 3, 5].

---

## 1. Structural Database Schema Extensions

These DDL definitions extend the Phase 2 Layer 1 data model. Execute these column additions using clean, non-destructive properties during your planned migrations.

```typescript
// db/schema/phase2-retrieval-extensions.ts
import { pgTable, uuid, text, integer, boolean, timestamp, index } from 'drizzle-orm/pg-core';
import { sql } from 'drizzle-orm';
import { brands } from './brands';
import { organizations } from './organizations';

// ALTER TABLE statements to inject columns into Phase 2 crawler_visit_logs table
// Part of the GA4 AI Traffic Attribution Core and Cloudflare Shield Detector
export const crawlerVisitLogsExtensions = {
  isActiveAgent: boolean('is_active_agent').default(false).notNull(), // True if browser session is active AI intent
  referrerAiSession: text('referrer_ai_session'), // 'chatgpt' | 'perplexity' | 'claude' | 'gemini'
  visitPurpose: text('visit_purpose'), // 'retrieval' | 'indexing' | 'training'[cite: 5]
  errorType: text('error_type'), // 'blocked_cdn' | 'js_render_fail' | '404' | null[cite: 5]
};

// ALTER TABLE statements to inject columns into Phase 2 content_structure_audits table
// Part of the B2B Agentic Commerce Readiness Metrics[cite: 5]
export const contentStructureAuditsExtensions = {
  outboundCitationCount: integer('outbound_citation_count').default(0).notNull(), // Foglift formatting signal[cite: 5]
  hasAuthorAttribution: boolean('has_author_attribution').default(false).notNull(), // Positive trust signal[cite: 5]
  citationProbabilityScore: numeric('citation_probability_score', { precision: 4, scale: 3 }), // 0.000-1.000 range[cite: 5]
};